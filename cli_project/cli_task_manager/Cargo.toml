[package]
name = "cli_task_manager"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.0", features = ["derive","cargo"] } # or without derive if you prefer
pickledb = "0.5.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
